'use client'

import React, { useState } from 'react'

const PromotList = ({}) => {

    return (
      <div className="bot-list">
        prompt-list !
        </div>
      )

}

export default React.memo(PromotList)